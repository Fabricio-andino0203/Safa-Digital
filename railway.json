{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan optimize && php artisan storage:link && php artisan migrate --force && /assets/start.sh"
  }
}
