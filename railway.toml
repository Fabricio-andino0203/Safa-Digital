[build]
builder = "nixpacks"

[deploy]
startCommand = "touch /app/storage/app/database.sqlite && php artisan migrate --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT"
